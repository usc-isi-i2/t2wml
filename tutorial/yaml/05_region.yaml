statementMapping:
  region:
    - range: C2:D9
      skip_columns:
        - =value[$col,1]=="Total area"
  template:
    item: =item[A, $row]
    property: =item[$col, 1, "property"]
    value: =value[$col, $row]
    qualifier:
      - property: PCropType
        value: =item[B, $row]